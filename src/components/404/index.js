import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        color: "var(--green)",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "80vh",
      }}
    >
      <h1>
        <strong>404</strong> Nenhuma Página Encontrada :(
      </h1>
    </div>
  );
};

export default NotFound;
